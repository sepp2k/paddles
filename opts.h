#ifndef OPTS_H
#define OPTS_H

#include <QColor>

namespace Opts {
    const int tick = 15;
    const int fieldHeight = 500;
    const int fieldWidth = 800;
    const int ballRadius = 10;
    const int speedIncTime = 500;
    const int startingSpeed = 5;
    const int maxSpeed = 10;
}

#endif // OPTS_H
